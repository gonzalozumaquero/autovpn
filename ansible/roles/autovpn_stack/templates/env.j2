# ansible/roles/autovpn_stack/templates/env.j2
APP_MODE=server

WG_HOST={{ wg_public_host }}
WG_PORT={{ wg_port }}
WG_SUBNET={{ wg_subnet }}
WG_DNS={{ wg_dns }}
JWT_SECRET={{ jwt_secret }}
TZ={{ timezone | default('Europe/Madrid') }}
TOTP_ISSUER={{ totp_issuer | default('AutoVPN') }}
WG_MODE={{ wg_mode | default('container') }}
WG_CONTAINER_NAME={{ wg_container_name | default('wireguard') }}

ADMIN_EMAIL={{ vault_admin_email }}
ADMIN_PASSWORD_HASH={{ vault_admin_password_hash }}

